{
  "name": "gogodeno-client",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prebootstrap": "lerna clean -y",
    "bootstrap": "lerna bootstrap",
    "start": "lerna run start --parallel",
    "build": "lerna run build --scope \"@gogodeno/client\""
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
